{
  "name": "rtlgraph-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test \"packages/*/test/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json",
    "check": "npm run typecheck && npm test",
    "golden": "UPDATE_GOLDEN=1 node --test packages/rtl-render/test/render.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "~5.9.3"
  },
  "engines": {
    "node": ">=22.18"
  }
}
