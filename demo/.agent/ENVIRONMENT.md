# NodeGraph — Agent Environment Report

> Auto-generated by the NodeGraph extension at activation.
> **AI agents: read this file to understand what tools are available on this machine.**
> Re-generated each time a `.nodegraph.json` file is opened.

Generated: `2026-09-20T09:54:17.639Z`

---

## System

| | |
|---|---|
| OS | Linux (`linux`) |
| Architecture | `x64` |
| Python | ✅ `Python 3.12.9` |
| Python command | `python3` |

---

## PDF Reading Capabilities

| Tool | Available | Notes |
|------|:---------:|-------|
| PyMuPDF (`fitz`) | ❌ | Install: `pip install pymupdf` |
| pdfplumber | ✅ | available |
| pdfminer | ✅ | available |
| poppler (`pdftotext`) | ✅ | CLI tool available |
| Ghostscript (`gs`) | ✅ | available |

---

## Image Processing Capabilities

| Tool | Available | Notes |
|------|:---------:|-------|
| Pillow (`PIL`) | ✅ | v12.2.0 — recommended |
| OpenCV (`cv2`) | ✅ | available |
| ImageMagick (`convert`) | ✅ | CLI tool available |

---

## Agent Recommendations

### Reading a PDF
Use pdfplumber:
```python
import pdfplumber
with pdfplumber.open("paper.pdf") as pdf:
    text = "\n".join(p.extract_text() or "" for p in pdf.pages)
```

### Extracting images from a PDF
Pillow is available but cannot extract from PDF directly. Use PyMuPDF for extraction.

---

*To refresh this report, reopen any `.nodegraph.json` file.*